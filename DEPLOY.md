# Complete replacement instructions

Do not mix these files with an older version. Delete the current repository contents, then upload the complete contents of this package in one commit. Keep `app.py`, `requirements.txt`, and `render.yaml` at the repository root.
